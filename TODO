API:
	Insert/Remove sections and rows.

Controls:
	Add DateTime editing for UIPicker's

Sliders:
	Render the left/right image

Sections:
	Support for rendering icons

Web View:
	Cache the web browser across instances?
		Perhaps in the Root?

Widgets:
	Support checkbox (more than one item checked)
	Image Chooser

Reflection binder:
	Support Collections as a source that becomes a radio button
	And support a [RadioSelect] property on an int to map
	that to the selected value

Ponder:
	When to support pickers?
