Sliders:
	Render the left/right image

Sections:
	Support for rendering icons

Web View:
	Cache the web browser across instances?
		Perhaps in the Root?
	Dispose the web browser after use?

Widgets:
	Support checkbox (more than one item checked)
	Image Chooser
	Text Entry
	Password Entry

Reflection binder:
	Create the Element tree and then set the values back after editing.